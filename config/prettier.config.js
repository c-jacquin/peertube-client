/**
 * @type {import('./types').PrettierConfig}
 */
const config = {
  singleQuote: true,
  arrowParens: 'avoid',
  semi: true,
  bracketSpacing: true,
  trailingComma: 'all',
  printWidth: 80,
};

module.exports = config;
